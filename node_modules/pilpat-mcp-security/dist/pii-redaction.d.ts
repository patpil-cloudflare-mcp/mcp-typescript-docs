/**
 * PII (Personally Identifiable Information) Redaction Module
 *
 * Detects and redacts sensitive personal information from text:
 * - Email addresses
 * - Phone numbers (US and Polish formats)
 * - Credit card numbers
 * - Social Security Numbers (SSN)
 * - PESEL (Polish National ID Numbers)
 * - Polish ID Cards (Dowód Osobisty)
 * - Polish Passports
 * - Bank account numbers
 * - Driver's license numbers
 * - Passport numbers
 * - IP addresses
 * - URLs with credentials
 *
 * Note: Public business data (NIP, REGON) and postal codes are NOT redacted
 * as they are publicly available and often required as input for legitimate queries.
 */
export interface PIIRedactionOptions {
    /**
     * Redact email addresses
     * Note: Default is false to preserve email addresses in legitimate business contexts
     * @default false
     */
    redactEmails?: boolean;
    /**
     * Redact phone numbers (US, international formats)
     * @default true
     */
    redactPhones?: boolean;
    /**
     * Redact credit card numbers
     * @default true
     */
    redactCreditCards?: boolean;
    /**
     * Redact Social Security Numbers
     * @default true
     */
    redactSSN?: boolean;
    /**
     * Redact PESEL (Polish National ID Number)
     * Format: 11 digits (YYMMDDNNNCS)
     * @default true
     */
    redactPESEL?: boolean;
    /**
     * Redact Polish ID Card numbers (Dowód Osobisty)
     * Format: 3 letters + 6 digits (e.g., ABC123456)
     * @default true
     */
    redactPolishIdCard?: boolean;
    /**
     * Redact Polish passport numbers
     * Format: 2 letters + 7 digits (e.g., AB1234567)
     * @default true
     */
    redactPolishPassport?: boolean;
    /**
     * Redact Polish phone numbers
     * Format: +48 XXX XXX XXX
     * @default true
     */
    redactPolishPhones?: boolean;
    /**
     * Redact bank account numbers
     * @default true
     */
    redactBankAccounts?: boolean;
    /**
     * Redact driver's license numbers
     * @default true
     */
    redactDriverLicense?: boolean;
    /**
     * Redact passport numbers
     * @default true
     */
    redactPassport?: boolean;
    /**
     * Redact IP addresses (IPv4 and IPv6)
     * @default true
     */
    redactIPAddresses?: boolean;
    /**
     * Redact URLs containing credentials (http://user:pass@host)
     * @default true
     */
    redactURLCredentials?: boolean;
    /**
     * Custom placeholder text for redaction
     * @default "[REDACTED]"
     */
    placeholder?: string;
}
export interface PIIDetectionResult {
    redacted: string;
    detectedPII: string[];
    detectionDetails: {
        emails: string[];
        phones: string[];
        creditCards: string[];
        ssns: string[];
        peselNumbers: string[];
        polishIdCards: string[];
        polishPassports: string[];
        polishPhones: string[];
        bankAccounts: string[];
        driverLicenses: string[];
        passports: string[];
        ipAddresses: string[];
    };
}
/**
 * Redact Personally Identifiable Information (PII) from text
 *
 * @param text - The text containing potential PII
 * @param options - Redaction options
 * @returns Object with redacted text, detected PII types, and details
 *
 * @example
 * ```typescript
 * const result = redactPII(
 *   'Contact me at john@example.com or 555-123-4567',
 *   { redactEmails: true, redactPhones: true }
 * );
 * // result.redacted: 'Contact me at [REDACTED] or [REDACTED]'
 * // result.detectedPII: ['email', 'phone']
 * ```
 */
export declare function redactPII(text: string, options?: PIIRedactionOptions): PIIDetectionResult;
/**
 * Check if text contains any PII
 *
 * @param text - Text to check
 * @returns True if any PII patterns detected
 */
export declare function hasPII(text: string): boolean;
/**
 * Get summary of PII found in text
 *
 * @param text - Text to analyze
 * @returns Summary of PII detection
 */
export declare function getPIISummary(text: string): {
    hasPII: boolean;
    piiTypes: string[];
    count: number;
    summary: string;
};
//# sourceMappingURL=pii-redaction.d.ts.map