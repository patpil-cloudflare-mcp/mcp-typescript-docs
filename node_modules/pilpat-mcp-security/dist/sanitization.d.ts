/**
 * Output Sanitization Module
 *
 * Removes potentially dangerous content from text responses:
 * - HTML/XML tags and script content
 * - Control characters
 * - Dangerous Unicode
 * - Length enforcement
 */
export interface SanitizationOptions {
    /**
     * Remove HTML/XML tags from output
     * @default true
     */
    removeHtml?: boolean;
    /**
     * Maximum length of output (truncates with "...")
     * @default undefined (no limit)
     */
    maxLength?: number;
    /**
     * Allowed HTML tags if removeHtml is false
     * Empty array means all tags removed
     * @default []
     */
    allowedTags?: string[];
    /**
     * Remove control characters (ASCII 0-31 except \n, \r, \t)
     * @default true
     */
    removeControlChars?: boolean;
    /**
     * Normalize whitespace (multiple spaces to single space)
     * @default true
     */
    normalizeWhitespace?: boolean;
    /**
     * Remove common encoding patterns that could indicate injections
     * @default true
     */
    removeEncodingPatterns?: boolean;
}
/**
 * Sanitize output text by removing potentially dangerous content
 *
 * @param text - The text to sanitize
 * @param options - Sanitization options
 * @returns Sanitized text
 *
 * @example
 * ```typescript
 * const raw = '<script>alert("XSS")</script>Hello World';
 * const safe = sanitizeOutput(raw, { removeHtml: true });
 * // Result: "Hello World"
 * ```
 */
export declare function sanitizeOutput(text: string, options?: SanitizationOptions): string;
/**
 * Detect if text contains common HTML/script injection patterns
 *
 * @param text - Text to check
 * @returns True if injection patterns detected
 */
export declare function hasInjectionPatterns(text: string): boolean;
/**
 * Get sanitization statistics (for monitoring)
 *
 * @param original - Original text
 * @param sanitized - Sanitized text
 * @returns Statistics about sanitization
 */
export declare function getSanitizationStats(original: string, sanitized: string): {
    originalLength: number;
    sanitizedLength: number;
    removedLength: number;
    removalPercentage: number;
    hadDangerousPatterns: boolean;
};
//# sourceMappingURL=sanitization.d.ts.map