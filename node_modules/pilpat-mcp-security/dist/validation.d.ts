/**
 * Output Validation Module
 *
 * Validates that tool responses conform to expected formats and constraints:
 * - Type validation (string, object, array)
 * - Schema compliance
 * - Size/length validation
 * - Content warnings
 */
export interface ValidationOptions {
    /**
     * Expected type of output
     * @default "string"
     */
    expectedType?: 'string' | 'object' | 'array' | 'any';
    /**
     * Maximum allowed size in bytes
     * @default undefined (no limit)
     */
    maxSizeBytes?: number;
    /**
     * Maximum string length
     * @default undefined (no limit)
     */
    maxLength?: number;
    /**
     * Warn if output looks like internal error details
     * @default true
     */
    warnOnErrorDetails?: boolean;
    /**
     * Warn if output contains database-like data
     * @default true
     */
    warnOnDatabaseData?: boolean;
    /**
     * Warn if output contains code snippets
     * @default false
     */
    warnOnCodeSnippets?: boolean;
    /**
     * Allowed top-level keys (if object validation)
     * Empty array = no validation
     * @default []
     */
    allowedKeys?: string[];
}
export interface ValidationResult {
    valid: boolean;
    errors: string[];
    warnings: string[];
    metadata: {
        type: string;
        size: number;
        length?: number;
        keys?: string[];
    };
}
/**
 * Validate output response format and content
 *
 * @param output - The output to validate
 * @param options - Validation options
 * @returns Validation result with errors and warnings
 *
 * @example
 * ```typescript
 * const result = validateOutput(toolOutput, {
 *   expectedType: 'object',
 *   maxSizeBytes: 10000,
 *   warnOnErrorDetails: true
 * });
 *
 * if (!result.valid) {
 *   console.error('Validation errors:', result.errors);
 * }
 * if (result.warnings.length > 0) {
 *   console.warn('Validation warnings:', result.warnings);
 * }
 * ```
 */
export declare function validateOutput(output: unknown, options?: ValidationOptions): ValidationResult;
/**
 * Strict validation that returns null if validation fails
 * Useful for failsafe operations
 *
 * @param output - Output to validate
 * @param options - Validation options
 * @returns Output if valid, null if invalid
 */
export declare function validateOutputStrict(output: unknown, options?: ValidationOptions): unknown | null;
/**
 * Get human-readable validation report
 *
 * @param output - Output to validate
 * @param options - Validation options
 * @returns Human-readable validation report string
 */
export declare function getValidationReport(output: unknown, options?: ValidationOptions): string;
//# sourceMappingURL=validation.d.ts.map