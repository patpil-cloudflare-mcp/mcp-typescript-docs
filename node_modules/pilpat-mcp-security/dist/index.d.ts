/**
 * @wtyczki/mcp-security
 *
 * Security utilities for Cloudflare MCP servers
 * Provides output sanitization, PII redaction, and response validation
 *
 * @example
 * ```typescript
 * import { sanitizeOutput, redactPII } from '@wtyczki/mcp-security';
 *
 * const result = await tool.execute(params);
 * const sanitized = sanitizeOutput(result);
 * const { redacted } = redactPII(sanitized);
 * return { content: [{ type: "text", text: redacted }] };
 * ```
 */
import type { SanitizationOptions } from './sanitization';
import type { PIIRedactionOptions } from './pii-redaction';
export { sanitizeOutput, type SanitizationOptions } from './sanitization';
export { redactPII, type PIIRedactionOptions, type PIIDetectionResult } from './pii-redaction';
export { validateOutput, type ValidationOptions, type ValidationResult } from './validation';
/**
 * Version info for debugging and diagnostics
 */
export declare const VERSION = "1.1.0";
/**
 * Combined sanitization and PII redaction in one call
 * Recommended for most use cases
 *
 * @example
 * ```typescript
 * const { sanitized, redacted, detectedPII } = secureOutput(rawResult, {
 *   sanitize: { removeHtml: true },
 *   redact: { redactEmails: true, redactPhones: true }
 * });
 * ```
 */
export declare function secureOutput(text: string, options?: {
    sanitize?: SanitizationOptions;
    redact?: PIIRedactionOptions;
}): {
    sanitized: string;
    redacted: string;
    detectedPII: string[];
};
//# sourceMappingURL=index.d.ts.map